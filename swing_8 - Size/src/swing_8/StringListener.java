package swing_8;

// Anything that implements the StringListener interface has to have a method called text emitted, that accepts a string.
public interface StringListener {
	
	public void textEmitted(String text);
	

}
